# DB-Legends-Farm-Bot
Farm Dragon Ball Legends events for event rewards.

## Run the GUI

This project includes a lightweight desktop control panel for choosing a farm
duration, starting and stopping a run, and rotating through configured events.

```bash
python -m dbl_farm_bot
```

## Test

```bash
python -m unittest discover
```
