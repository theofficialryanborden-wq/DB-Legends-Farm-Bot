# AGENTS.md

## Cursor Cloud specific instructions

This repository (`DB-Legends-Farm-Bot`) is currently an empty project stub. It contains only a `README.md` describing the project intent: farming Dragon Ball Legends events for event rewards.

### Current state

- No source code, frameworks, or dependencies exist yet.
- No package manager, lockfile, or build system is configured.
- No tests, lint configuration, or CI pipeline is present.
- No services or databases are required.

### When code is added

Once the project gains source code:

- Check the root for dependency files (`package.json`, `requirements.txt`, `pyproject.toml`, etc.) and install accordingly.
- Look for a `Makefile`, `scripts/` directory, or documented dev commands in `README.md`.
- Update the VM update script via `SetupVmEnvironment` to include the correct dependency installation command.
